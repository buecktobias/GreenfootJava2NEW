import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;


class MovingActorTest {
    @Test
    void hasPosition(){
        MyWorld mw = new MyWorld();
        //Snake sk = new Snake();
        //mw.addObject(sk, 100, 100);
        //assertEquals(sk.getPosition(), new Vector2D(100, 100));
    }

}